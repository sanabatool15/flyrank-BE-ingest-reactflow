# OpenAI
OPENAI_API_KEY=

# Inngest
# Set to 1 for local development so the SDK talks to the local Inngest Dev
# Server instead of expecting production signing keys.
INNGEST_DEV=1

# Only needed when deploying (not for local dev)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=
